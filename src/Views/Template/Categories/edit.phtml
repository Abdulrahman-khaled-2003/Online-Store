<!DOCTYPE html>
<html lang="en">
<?php
require base_path("Views/Partials/head.phtml") ?>

<body>
    <?php require base_path("Views/Partials/nav.phtml") ?>
    <div class="min-h-screen flex items-center justify-center bg-gray-100 px-4">
        <form method="POST" action="/categories/update" class="bg-white shadow-xl rounded-2xl p-8 w-full max-w-md space-y-6">
            <input type="hidden" name="_method" value="PUT">
            <input type="hidden" name="id" value="<?= $category['id'] ?>">
            <h2 class="text-2xl font-bold text-indigo-700 text-center mb-2">Edit Category</h2>
            <div>
                <label for="category-name" class="block text-sm font-semibold text-indigo-700 mb-1">Category Name</label>
                <input
                    type="text"
                    id="category-name"
                    name="category-name"
                    value="<?= htmlspecialchars($category['categoryName']) ?>"
                    class="w-full px-4 py-2 border border-indigo-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-400 transition"
                    placeholder="e.g. Electronics">
                <p class="text-red-500 text-md"><?= $error['categoryName'] ?? "" ?></p>

            </div>
            <div>
                <label for="category-desc" class="block text-sm font-semibold text-indigo-700 mb-1">Description</label>
                <textarea
                    id="category-desc"
                    name="category-desc"
                    rows="3"
                    class="w-full px-4 py-2 border border-indigo-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-400 transition resize-none"
                    placeholder="Brief description of the category"><?= htmlspecialchars($category['categoryDescription']) ?></textarea>
                <p class="text-red-500 text-md"><?= $error['categoryDescription'] ?? "" ?></p>

            </div>
            <div>
                <label class="block text-sm font-medium text-indigo-600">Product Image</label>

                <!-- Upload new image -->
                <input type="file" name="category-image"
                    class="mt-2 w-full text-sm text-gray-700 
               file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 
               file:text-sm file:font-semibold 
               file:bg-indigo-50 file:text-indigo-700 
               hover:file:bg-indigo-100 cursor-pointer" />
                <p class="text-red-500 text-md"></p>
            </div>
            <p class="text-red-500 text-me"><?= $errors['categoryImage'] ?? "" ?></p>
            <?php if (!empty($product['productImage'])): ?>
                <!-- Show old image -->
                <div class="mb-3">
                    <img src="/assets/images/<?= htmlspecialchars($product['productImage']) ?>"
                        alt="Old Product Image"
                        class="w-32 h-32 object-cover rounded-lg shadow-md border" />
                    <p class="text-gray-500 text-sm mt-1">Current Image</p>
                </div>
            <?php endif; ?>
            <button
                type="submit"
                class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 rounded-lg transition text-lg shadow-md">
                Update Category
            </button>
        </form>
    </div>
</body>

</html>